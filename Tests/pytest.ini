[pytest]
testpaths = ./Tests # directory with test_*.py files
pythonpath = ./SRC # add SRC to sys.path for imports
addopts = -q 
filterwarnings =
    ignore::Bio.BiopythonDeprecationWarning
    ignore::DeprecationWarning:Bio\.
